// function definition of printHello() in test.c
//
#include <stdio.h>  // The < > brackets are used for system header files.
#include "test.h"   // The " " quotes are used for user-defined header files.

void printHello(){
    printf("Hello World\n");
}
int add(int a, int b){
    return a + b;
}